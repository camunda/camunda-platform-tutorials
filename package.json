{
  "name": "camunda-8-tutorials",
  "private": true,
  "scripts": {
    "lint": "find quick-start solutions -name '*.bpmn' -not -path '*/test/*' -not -path '*/target/*' | xargs bpmnlint --config .bpmnlintrc"
  },
  "devDependencies": {
    "bpmnlint": "^11.12.1",
    "bpmnlint-plugin-camunda-compat": "^2.52.0",
    "zeebe-bpmn-moddle": "^1.14.0"
  }
}
