name: BPMN Lint

on:
  pull_request:
    paths:
      - '**/*.bpmn'
      - '**/*.dmn'
      - '.bpmnlintrc'
      - 'package.json'
      - 'package-lock.json'

jobs:
  lint:
    name: Lint BPMN/DMN files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Install xmllint
        run: sudo apt-get install -y libxml2-utils

      - name: Find changed BPMN/DMN files
        id: changed
        run: |
          ALL=$(git diff --name-only ${{ github.event.pull_request.base.sha }}...${{ github.sha }} \
            | grep -E '\.(bpmn|dmn)$' \
            | grep -v '/target/' \
            | tr '\n' ' ')
          BPMN_ONLY=$(git diff --name-only ${{ github.event.pull_request.base.sha }}...${{ github.sha }} \
            | grep -E '\.bpmn$' \
            | grep -v '/target/' \
            | tr '\n' ' ')
          echo "files=$ALL" >> "$GITHUB_OUTPUT"
          echo "bpmn_files=$BPMN_ONLY" >> "$GITHUB_OUTPUT"
          echo "Changed BPMN/DMN files: $ALL"
          echo "Changed BPMN-only files: $BPMN_ONLY"

      - name: Check XML well-formedness (changed files only)
        if: steps.changed.outputs.files != ''
        run: |
          ERRORS=0
          for file in ${{ steps.changed.outputs.files }}; do
            if [ -f "$file" ]; then
              if ! xmllint --noout "$file" 2>/tmp/xmllint-err; then
                echo "::error file=$file::$(cat /tmp/xmllint-err)"
                ERRORS=$((ERRORS + 1))
              fi
            fi
          done
          if [ $ERRORS -gt 0 ]; then
            echo "$ERRORS BPMN file(s) failed XML well-formedness check."
            exit 1
          fi

      - name: Run bpmnlint (BPMN files only)
        if: steps.changed.outputs.bpmn_files != ''
        run: npx bpmnlint --config .bpmnlintrc ${{ steps.changed.outputs.bpmn_files }}
